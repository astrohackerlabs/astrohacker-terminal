# GTUI

Compatibility graphical TUI CLI for Astrohacker Terminal.

This crate builds the `termsurf` binary. The binary name is retained as a
TermSurf app-platform/protocol compatibility surface while the product is
Astrohacker Terminal.

Useful commands from `rust/`:

```sh
cargo check -p gtui
cargo build -p gtui
```
