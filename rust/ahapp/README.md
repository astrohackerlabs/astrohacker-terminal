# ahapp

In-terminal app launcher CLI for Astrohacker Terminal (GTUI).

Package folder, Cargo package name, and binary are all **`ahapp`**.

Useful commands from `rust/`:

```sh
cargo check -p ahapp
cargo build -p ahapp
```
